# Storage Directory

This directory contains persistent file uploads (payment proofs, etc.)

- `uploads/` — Payment proof images uploaded by participants

This directory is mounted as a Docker volume in docker-compose.yml.
