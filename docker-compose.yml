services:
  # PostgreSQL 17
  postgres:
    image: postgres:17-alpine
    container_name: gatherhub_db
    restart: unless-stopped
    environment:
      POSTGRES_DB: gatherhub
      POSTGRES_USER: gatherhub
      POSTGRES_PASSWORD: gatherhub
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backend/migrations:/docker-entrypoint-initdb.d
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U gatherhub -d gatherhub"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Backend (Go + Fiber)
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: gatherhub_backend
    restart: unless-stopped
    environment:
      APP_PORT: 3000
      APP_ENV: production
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USER: gatherhub
      DB_PASSWORD: gatherhub
      DB_NAME: gatherhub
      DB_SSLMODE: disable
      UPLOAD_DIR: /app/storage/uploads
      PAYMENT_UPLOAD_DIR: /app/storage/payments
      FRONTEND_DIR: /app/frontend
      ADMIN_USERNAME: admin
      ADMIN_PASSWORD: admin123
      SESSION_SECRET: gatherhub-secret-change-in-production
    ports:
      - "3000:3000"
    volumes:
      - ./storage:/app/storage
      - ./frontend:/app/frontend:ro
    depends_on:
      postgres:
        condition: service_healthy

  # Nginx Reverse Proxy
  nginx:
    image: nginx:alpine
    container_name: gatherhub_nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - ./frontend:/usr/share/nginx/html:ro
      - ./storage/uploads:/var/www/uploads:ro
      - ./storage/payments:/var/www/payments:ro
    depends_on:
      - backend

volumes:
  postgres_data:
    driver: local

networks:
  default:
    name: gatherhub_network
